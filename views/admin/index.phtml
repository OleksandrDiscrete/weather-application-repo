<section class="admin py-5">
    <div class="container">
        <div class="admin__wrap">

            <div class="text-content mb-4 d-flex justify-content-between align-items-center">
                <h1 class="mb-0">Вітаємо, <span class="text-primary">
                        <?= htmlspecialchars($adminName) ?>!
                    </span></h1>
                <a href="/admin/export" class="btn btn-outline-success">
                    <i class="bi bi-download me-2"></i> Експортувати міста
                </a>
            </div>

            <?php if (!empty($error)): ?>
                <div class="alert alert-danger">
                    <?= htmlspecialchars($error) ?>
                </div>
            <?php endif; ?>
            <?php if (!empty($message)): ?>
                <div class="alert alert-success">
                    <?= htmlspecialchars($message) ?>
                </div>
            <?php endif; ?>

            <div class="card shadow p-4 mx-auto mb-4 border-warning">
                <h2 class="mb-3 text-warning"><i class="bi bi-exclamation-triangle-fill me-2"></i>Система попереджень
                </h2>
                <form method="POST" action="/admin/alert">
                    <div class="mb-3">
                        <label for="alert_html" class="form-label">Введіть HTML-код попередження (використовуйте
                            &lt;strong&gt; та &lt;em&gt;)</label>
                        <textarea class="form-control" id="alert_html" name="alert_html" rows="3"
                            placeholder="Наприклад: <strong>Увага!</strong> Очікується <em>сильний вітер</em>."><?= htmlspecialchars($currentAlertHtml) ?></textarea>
                        <div class="form-text">Після збереження цей HTML буде перетворено у текстовий файл за допомогою
                            регулярних виразів. Щоб видалити банер, просто очистіть це поле.</div>
                    </div>
                    <button type="submit" class="btn btn-warning">Зберегти попередження</button>
                </form>
            </div>

            <div class="card shadow p-4 mx-auto mb-4">
                <h2 class="mb-4"><i class="bi bi-bar-chart-fill me-2 text-primary"></i>Лічильник відвідувань</h2>

                <div class="row text-center mb-4 g-3">
                    <div class="col-md-4">
                        <div class="border rounded p-3 h-100 bg-light">
                            <div class="fs-1 fw-bold text-primary">
                                <?= $statistics['total'] ?>
                            </div>
                            <div class="text-muted">Всього відвідувань</div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="border rounded p-3 h-100 bg-light">
                            <div class="fs-1 fw-bold text-success">
                                <?= $statistics['unique'] ?>
                            </div>
                            <div class="text-muted">Унікальних відвідувачів</div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="border rounded p-3 h-100 bg-light">
                            <div class="fs-1 fw-bold text-warning">
                                <?= $statistics['today'] ?>
                            </div>
                            <div class="text-muted">Відвідувань сьогодні</div>
                        </div>
                    </div>
                </div>

                <h5 class="mb-3">Статистика по сторінках</h5>
                <div class="table-responsive mb-4">
                    <table class="table table-bordered table-hover table-sm">
                        <thead class="table-dark">
                            <tr>
                                <th>Сторінка</th>
                                <th>Відвідувань</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($statistics['pageStats'] as $row): ?>
                                <tr>
                                    <td><code><?= htmlspecialchars($row['page']) ?></code></td>
                                    <td><span class="badge bg-primary">
                                            <?= (int) $row['visits'] ?>
                                        </span></td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>

                <h5 class="mb-3">Останні 5 відвідувань</h5>
                <div class="table-responsive">
                    <table class="table table-bordered table-hover table-sm">
                        <thead class="table-dark">
                            <tr>
                                <th>Сторінка</th>
                                <th>IP-адреса</th>
                                <th>Час</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($statistics['recent'] as $row): ?>
                                <tr>
                                    <td><code><?= htmlspecialchars($row['page']) ?></code></td>
                                    <td>
                                        <?= htmlspecialchars($row['ip_address']) ?>
                                    </td>
                                    <td>
                                        <?= htmlspecialchars($row['visited_at']) ?>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
            </div>

            <div class="card shadow p-4 mx-auto">
                <h2 class="mb-4">Додати нове місто</h2>
                <form method="POST" action="/admin/city">
                    <div class="mb-3">
                        <label for="name" class="form-label">Назва міста*</label>
                        <input type="text" class="form-control" id="name" name="name" minlength="2" maxlength="100"
                            placeholder="Введіть назву міста" required>
                    </div>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="positionX" class="form-label">Широта (Latitude)*</label>
                            <input type="number" step="0.0001" class="form-control" id="positionX" name="positionX"
                                min="-90" max="90" value="0" required>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="positionY" class="form-label">Довгота (Longitude)*</label>
                            <input type="number" step="0.0001" class="form-control" id="positionY" name="positionY"
                                min="-180" max="180" value="0" required>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label for="infoUrl" class="form-label">URL інформації про місто</label>
                        <input type="url" class="form-control" id="infoUrl" name="infoUrl"
                            placeholder="Введіть URL інформації про місто">
                    </div>
                    <div id="requiredFieldsHint" class="form-text mb-2">Всі поля позначені * є обов'язковими.</div>
                    <button type="submit" class="btn btn-primary">Зберегти місто</button>
                </form>
            </div>

        </div>
    </div>
</section>